# 中 V 随机推荐插件开发计划

## 1. 目标与范围

拟新增插件目录：`plugins/chinese_vocaloid_recommender/`。

插件负责四件事：

1. 群友提交 B 站中 V 曲目链接。
2. 保存曲目及上传者信息，并自动去重。
3. 随机推荐一首已收录且未删除的曲目。
4. 允许管理员或原上传者删除曲目。

首版不做网页后台、排行榜、搜索、审核队列和多平台曲源。插件回复采用短句，不接入 LLM，避免命令结果被二次发挥。

## 2. 是否需要服务器和数据库

### 2.1 服务器：首版不需要

MaiBot 所在电脑本身就是运行端。插件可以直接：

- 接收 QQ 命令；
- 请求 B 站视频信息和封面；
- 通过现有 `ctx.send.hybrid` 能力发送“文字 + 图片 + 文字”；
- 在插件自己的数据目录保存记录。

因此无需购买云服务器、域名，也无需开放端口。只要 MaiBot 所在电脑在线并能访问 B 站即可。

只有出现以下需求时，才考虑增加服务器：多台机器人共享同一个曲库、机器人关机后仍需网页投稿、需要远程管理后台，或曲库规模大到必须集中维护。

### 2.2 数据库：需要，但使用本地 SQLite

推荐使用 Python 自带的 SQLite。它表现为插件数据目录里的一个文件，无需安装 MySQL，也无需单独启动数据库服务。

建议位置：

```text
data/plugins/welken.chinese-vocaloid-recommender/
├── tracks.sqlite3
└── covers/
    └── BVxxxxxxxxxx.jpg
```

不用 JSON 保存曲库，原因是本功能需要唯一去重、按 ID 删除、权限核对、随机查询和并发写入；SQLite 对这些操作更稳。也不直接修改 `MaiBot.db`，避免插件表和 MaiBot 主程序的数据库迁移互相影响。

封面图片保存在 `covers/`，数据库只保存路径和来源地址，不把大块图片写进数据库。

### 2.3 需要安装的内容

首版不需要安装任何新包：

- B 站元数据直接请求固定地址 `https://api.bilibili.com/x/web-interface/view`；
- HTTP 请求使用 MaiBot 已有的 `aiohttp`；
- SQLite 是 Python 标准库 `sqlite3` 的一部分，不需要另外下载、安装或启动；
- 不需要 `bilibili-api-python`、MySQL、Redis、Docker、Web 服务器、B 站 Cookie、API Key、代理或 `curl_cffi`。

这样不会引入已经停止维护的 `bilibili-api-python` 及其间接依赖，也不会修改 `pyproject.toml` 和 `uv.lock`。

当前仓库的 `.venv` 记录为 `uv` 管理的 Python 3.13.5，但本次检查无法从该环境启动解释器。实现前先以“MaiBot 当前是否能正常启动”为准：若能启动，不重建环境；若也无法启动，再用 `uv sync` 修复项目环境。不要把依赖单独装到系统 Python 3.14 中，否则插件进程可能仍然找不到它。

## 3. 命令设计

| 功能 | 命令 | 参数 | 权限 | 行为 |
| --- | --- | --- | --- | --- |
| 随机推荐 | `/中v推荐`、`/随机中v` | 无 | 所有人 | 随机返回一首有效曲目 |
| 上传曲目 | `/上传中v <B站链接或BV号>` | B 站视频链接或裸 BV/AV 号 | 所有人，有频率限制 | 校验标识或链接、读取视频信息、去重并收录 |
| 删除曲目 | `/中v删除 <ID>` | 正整数曲目 ID | 管理员或原上传者 | 软删除指定曲目 |
| 帮助 | `/中v帮助` | 无 | 所有人 | 显示精简命令说明 |

全部命令必须以半角 `/` 开头，不注册任何无 `/` 的别名。命令里的英文字母 `v` 大小写均可，命令首尾允许空格，不接受缺失参数或多余正文。另设一个仅覆盖上述命令名的错误参数处理器，让 `/中v删除`、`/上传中v` 等格式错误的命令返回用法提示并被拦截，不落入普通聊天。

建议正则：

```python
r"(?i)^/(?:中v推荐|随机中v)\s*$"
r"(?i)^/上传中v\s+(?P<url>\S+)\s*$"
r"(?i)^/中v删除\s+(?P<track_id>\d+)\s*$"
r"(?i)^/中v帮助\s*$"
```

所有命令处理完返回 `intercept=True`，不再进入 Lynora 的普通聊天流程。

### 3.1 随机推荐的发送格式

实现进度（2026-09-23）：M6 已完成。M6a 实现纯文本随机推荐、按聊天流近期排除和曲库不足回退；M6b 实现封面图文、元数据按需刷新、按聊天流冷却及失败时纯文本降级。

使用一条图文混合消息，顺序固定为：

```text
推荐
《曲目标题》
[B 站视频封面]
https://www.bilibili.com/video/BVxxxxxxxxxx
```

发送时优先使用 `ctx.send.hybrid`，把文本、图片、链接放进同一条消息。封面发送失败时，仍发送标题和链接，不让整个推荐失败。

随机规则：

- 只从 `active` 状态的曲目中选择；
- 默认避开当前聊天最近推荐过的 5 首，曲库不足时自动放宽；
- 曲库为空时回复：`曲库还没歌，先 /上传中v 传一首`；
- 每个聊天流默认 5 秒冷却，防止连续刷屏。

### 3.2 上传流程

1. 检查用户上传冷却和每日上限。
2. 只接受明确允许的 B 站域名和 HTTPS 链接。
3. 展开 `b23.tv` 短链接，但每次跳转都重新校验域名，最多跳转 3 次。
4. 从链接提取 BV/AV 号，转换为统一的标准链接。
5. 使用 `aiohttp` 请求固定的 B 站 `x/web-interface/view` 接口，通过 `bvid` 或 `aid` 参数读取标题、封面地址、BVID、AID 和播放量。
6. 检查数据库是否已有相同 BVID/AVID。
7. 在数据库唯一约束保护下写入曲目、上传者、来源聊天和时间。
8. 下载并校验封面，使用视频 ID 作为文件名；失败时静默保留曲目并标记为待补缓存。
9. 回复：`收录成功，ID 27\n《曲目标题》`。

首版无法可靠判断一首歌是否“确实属于中 V”。视频标签只能辅助判断，不能作为绝对标准。因此首版由上传者对内容负责，管理员通过删除命令纠错。后续若垃圾投稿较多，再增加待审核状态。

建议默认限流：

- 同一用户两次上传尝试至少间隔 60 秒；
- 同一用户每天最多成功上传 10 首；
- 同一聊天流每分钟最多处理 20 次上传尝试；
- 数值全部写入配置文件，后续可以直接修改。

典型回复：

```text
收录成功，ID 27
《标题》

已在曲库中，ID 12

链接不对，只收B站视频链接

缓会，43秒后再传
```

### 3.3 封面获取、缓存与清理

B 站视频信息接口返回的 `data.pic` 是封面地址。插件需要同时保存封面链接和一份本地缓存，二者用途不同：

- `cover_url`：保存在数据库中，用于本地文件丢失或损坏时重新下载；
- `cover_path`：指向本地缓存文件，推荐时优先读取并转为 base64 发送；
- 不直接把远程图片 URL 交给 QQ，也不在每次推荐时重复下载，因为现有 MaiBot 图文发送能力需要实际图片数据，且远程地址可能超时或失效。

上传时的处理顺序：

1. 从视频信息的 `pic` 字段取得封面地址，并把 `http://` 规范为 `https://`；
2. 只允许 B 站图片域名，限制响应体最大 8 MiB；
3. 下载到临时文件，校验 JPEG/PNG/GIF/WEBP 文件头；
4. 使用 BVID 生成固定文件名，再原子移动到 `covers/`；
5. 下载失败不阻止曲目入库，但标记为待补缓存。

推荐时的回退顺序：

1. 本地缓存存在且校验通过：直接发送；
2. 本地缓存缺失：使用数据库中的 `cover_url` 重下；
3. 原封面链接失效：重新请求视频信息接口，刷新标题和封面地址后再下载；
4. 仍然失败：发送纯文本标题与视频链接。

清理规则采用“保留有效封面，清理可恢复缓存”：

- `active` 曲目的封面不按天数删除，否则推荐时会反复访问 B 站；
- 曲目软删除后，封面再保留 30 天，给误删恢复留出时间；
- `.tmp` 临时文件超过 24 小时删除；
- 没有任何数据库记录引用的孤儿封面超过 7 天删除；
- 每 24 小时最多执行一次惰性清理，由收到命令时触发，不为此另建服务器或定时服务；
- 默认缓存上限 256 MiB。超过上限时先删孤儿和已删除曲目的缓存，再按最久未使用顺序淘汰有效曲目的本地副本；只删可重新下载的图片，不删曲目记录和 `cover_url`。

数据库增加 `cover_cached_at`、`cover_last_accessed_at` 和 `cover_status` 字段，用于清理和失败重试。

### 3.4 删除与权限

删除采用“软删除”：曲目仍留在数据库中用于审计，但不会再被推荐。这样误删后可以人工恢复，也能防止删掉再反复上传绕过去重。

允许删除的身份按以下顺序判断：

1. 原上传者：`user_id` 与记录中的 `uploader_id` 一致；
2. 插件配置中的全局管理员：可删除任意曲目；
3. QQ 群主或群管理员：通过 NapCat 的群成员信息接口确认，但只允许删除“在当前群提交”的曲目，并且删除命令也必须在该群内发出。

如果 NapCat 权限查询失败，按最小权限处理，不把普通成员误判为管理员；原上传者和配置中的全局管理员仍可删除。私聊中不存在群管理员身份，只认原上传者和全局管理员。

曲库确定为跨群共享：任何允许使用插件的群都可能随机到全部有效曲目。`origin_group_id` 只用于审计和限制群管理员的删除范围，不用于隔离推荐结果。私聊提交的曲目没有来源群，群管理员无权删除。

典型回复：

```text
删了，ID 27

没这个ID

这首已经删过了

这首不是你传的
```

### 3.5 帮助内容

帮助保持短，不展示内部规则：

```text
/中v推荐：随机来一首
/上传中v <B站链接或BV号>：收录曲目
/中v删除 <ID>：删除自己上传的曲目
```

## 4. 数据设计

### 4.1 `tracks` 曲目表

| 字段 | 用途 | 是否需要 |
| --- | --- | --- |
| `id` | 自增数字 ID，供删除命令使用 | 需要，作为数字 ID |
| `bvid` | B 站 BV 号，唯一索引 | 需要，唯一索引 |
| `aid` | B 站 AV 号，唯一索引 | 需要；用户输入可只有 BV 或 AV，API 校验成功后两者都应存在 |
| `canonical_url` | 规范化后的 B 站链接 | 需要 |
| `title` | 视频标题 | 需要 |
| `cover_url` | B 站封面来源地址 | 需要 |
| `cover_path` | 本地封面相对路径 | 需要；尚未缓存或缓存失败时可为空 |
| `cover_status` | `cached`、`missing` 或 `failed` | 需要 |
| `cover_cached_at` | 最近成功缓存时间 | 需要；未成功缓存时为空 |
| `cover_last_accessed_at` | 最近推荐读取时间 | 需要；尚未推荐时为空 |
| `video_state` | B 站视频状态，用于基础有效性检查 | 需要 |
| `view_count` | 播放量；上传时取得，超过刷新周期后按需更新 | 需要 |
| `metadata_refreshed_at` | 最近成功读取 B 站元数据的时间 | 需要，作为播放量刷新判断依据 |
| `uploader_id` | QQ 上传者 ID，权限判断依据 | 需要，但不展示 |
| `origin_group_id` | 来源群；私聊上传时为空 | 需要，作为群管理员删除权限的前置条件 |
| `origin_stream_id` | 来源聊天流 | 需要 |
| `status` | `active` 或 `deleted` | 需要 |
| `created_at` | 收录时间 | 需要 |
| `deleted_at` | 删除时间 | 需要；未删除时为空 |
| `deleted_by` | 执行删除的 QQ 用户 ID | 需要；未删除时为空 |

首版不保存视频时长、B 站投稿者名称/UID、上传者昵称或点赞数，因为它们既不展示，也不参与权限判断。删除这些字段可以减少不必要的个人信息和刷新工作。

### 4.2 辅助表

- `recommendation_history`：记录聊天流、曲目 ID 和推荐时间，用于减少短期重复；定期清理 30 天前记录。
- `command_events`：记录上传尝试和成功时间，用于跨重启限流；只保留限流窗口需要的数据。
- `schema_meta`：保存数据库版本，便于以后升级字段。

SQLite 启用 WAL、`busy_timeout` 和参数化 SQL；写操作使用短事务，并在插件内用 `asyncio.Lock` 串行化。`bvid` 和 `aid` 的数据库唯一约束是并发去重的最终保障：几乎同时上传同一视频时，只有第一次插入成功，后续请求捕获唯一约束冲突并返回同一个已有数字 ID。

## 5. 配置项

建议 `config.toml` 至少包含：

```toml
[plugin]
enabled = true
config_version = "0.1.0"

[scope]
allowed_group_ids = []       # 空数组表示不限制群
allow_private = true
library_mode = "global"      # 已确定：跨群共享曲库

[permission]
global_admin_ids = []
allow_group_admin_delete = true

[rate_limit]
upload_cooldown_seconds = 60
upload_daily_limit = 10
stream_upload_attempts_per_minute = 20
recommend_cooldown_seconds = 5

[recommendation]
recent_exclusion_count = 5
metadata_refresh_days = 2       # 0 表示每次查询都尝试刷新，不建议日常使用

[network]
request_timeout_seconds = 12
max_redirects = 3
metadata_max_bytes = 2097152
max_cover_bytes = 8388608

[cover_cache]
cleanup_interval_hours = 24
deleted_retention_days = 30
orphan_retention_days = 7
temp_retention_hours = 24
max_cache_mib = 256
```

全局管理员必须配置 QQ 号，不能只按昵称判断。`allowed_group_ids` 可在试运行时只填测试群，稳定后再放开。

## 6. B 站视频信息接口与安全边界

### 6.1 接口是否足够

首版直接使用：

```text
GET https://api.bilibili.com/x/web-interface/view?bvid=<BVID>
GET https://api.bilibili.com/x/web-interface/view?aid=<AID>
```

2026-09-21 对样例 `BV16veP6eEeC` 做了只读实测：BVID 和 AID 两种查询均返回 `code=0`，并提供本插件需要的字段。

| 返回字段 | 插件用途 |
| --- | --- |
| `code`、`message` | 判断接口业务成功或失败；HTTP 200 不等于业务成功 |
| `data.bvid`、`data.aid` | 生成标准链接和唯一去重 |
| `data.title` | 推荐时显示标题 |
| `data.pic` | 获取封面地址 |
| `data.state` | 基础有效性检查和审计 |
| `data.stat.view` | 保存并按刷新周期更新播放量 |
| `data.videos`、`data.pages` | 识别多分 P 视频，首版仍按整个视频收录 |
| `data.pubdate`、`data.tid`、`data.tname` | 可选审计信息，不参与权限判断 |

无效 BVID 或缺少参数时，实测返回 `code=-400`、`message=请求错误`。接口返回的封面将 `http://` 改成 `https://` 后，样例封面可直接下载，响应为 JPEG，大小约 157 KiB。

结论：这个接口足够完成首版的“验证视频、取得标题、封面、播放量和统一 ID”，不再需要 `bilibili-api-python`。它不提供可靠的“是否属于中 V”判断，也不是承诺长期稳定的插件接口，因此仍要隔离在 `bilibili_client.py`，其余模块只接收插件自己的 `VideoMetadata`。

### 6.2 请求与响应规则

- 普通 `www.bilibili.com/video/BV...` 链接只在本地解析 BVID，不请求用户提供的 URL；
- AV 链接在本地解析纯数字 AID，再把它作为 `aid` 参数请求固定接口；
- `b23.tv` 必须请求短链才能展开，最多 3 次跳转，每次都检查目标域名；
- 请求参数由 `aiohttp` 的 `params` 构造，不手工拼接用户输入；
- 固定发送合理的 `User-Agent` 和 B 站 `Referer`，总超时 12 秒；
- 限制元数据响应最大 2 MiB，先检查 JSON 类型，再检查 `code == 0` 和 `data`；
- `bvid`、`aid`、`title`、`pic` 任一核心字段类型错误时拒绝入库；
- 对 412、429、5xx、超时和非 JSON 响应统一做失败降级，不自动高频重试；
- 上传时取得的信息写入本地库；推荐或按 ID 查询时先检查 `metadata_refreshed_at`，默认超过 2 天才请求一次视频接口；
- 刷新成功时更新标题、封面来源、视频状态、播放量和刷新时间；刷新失败时继续使用库内旧信息完成推荐，不因 B 站临时故障中断核心功能；
- `metadata_refresh_days = 0` 可近似实时刷新，但会使每次查询依赖外部接口并增加触发限流的概率，因此不作为默认值。

### 6.3 安全边界

- 允许域名采用精确白名单，例如 `bilibili.com` 的明确子域和 `b23.tv`，不能用简单的“字符串包含 bilibili”判断；
- 禁止访问用户提供的任意主机、IP 地址和非 HTTPS 地址，防止链接被用于探测机器人所在内网；
- 网络请求设置总超时、响应大小上限和浏览器 User-Agent；
- 标题移除控制字符并限制保存长度；
- 封面按 JPEG/PNG/GIF/WEBP 文件头校验，不能只相信响应的 `Content-Type`；
- SQL 全部使用占位参数，不能拼接用户输入；
- 日志不记录 Cookie、鉴权信息或完整异常响应；
- B 站接口不可用时明确提示稍后再试，不写入半条数据。

首版不使用 B 站登录 Cookie、API Key、代理、风控绕过或任何写操作。如果公共视频信息无法匿名读取，插件应失败并提示稍后再试，而不是自动引入账号凭据。

## 7. 插件文件结构

```text
plugins/chinese_vocaloid_recommender/
├── _manifest.json
├── config.toml
├── plugin.py                 # 命令入口和回复编排
├── repository.py             # SQLite 建表、查询和事务
├── bilibili_client.py        # aiohttp 请求固定 view API，输出内部元数据对象
├── cover_cache.py            # 封面下载、读取、补抓和惰性清理
├── permissions.py            # 上传者/全局管理员/群管理员判断
├── models.py                 # 插件内部数据对象
├── README.md
└── tests/
    ├── test_urls.py
    ├── test_repository.py
    ├── test_permissions.py
    └── test_commands.py
```

Manifest 预计声明 `send.text`、`send.image`、`send.hybrid` 和调用 NapCat API 所需能力。B 站元数据直接使用项目已有的 `aiohttp` 请求固定接口；数据库使用 Python 标准库 `sqlite3`，不增加任何第三方依赖或数据库服务。

## 8. 错误处理和降级

| 场景 | 处理 |
| --- | --- |
| B 站链接无法解析 | 不入库，提示只接受 B 站视频链接 |
| B 站接口超时或限流 | 不入库，提示稍后再试 |
| B 站接口返回非 JSON 或缺少核心字段 | 不入库，记录精简日志并提示稍后再试 |
| 曲目重复 | 返回已有 ID，不重复下载封面 |
| 封面下载失败 | 允许收录；推荐时尝试重新获取，最终可退化为纯文本 |
| 数据库暂时被占用 | 短暂重试，仍失败则提示稍后再试 |
| NapCat 管理员查询失败 | 不授予群管理员权限 |
| 图文混合发送失败 | 退化为标题加链接，避免完全无回复 |
| 数据库或封面目录不可写 | 插件启动时报错并禁用写命令，不伪装成上传成功 |

## 9. 实施顺序

1. 整理现有草稿，使插件先能通过语法检查并正常加载空壳。
2. 完成配置模型、`config.toml`、Manifest 和帮助命令。
3. 实现 SQLite 建表、版本迁移、曲目增删查和唯一约束。
4. 实现 B 站链接白名单、短链展开和固定 `view` API 客户端。
5. 使用真实公开视频及模拟异常响应验证元数据转换。
6. 实现封面下载、缓存回退、容量限制和惰性清理。
7. 实现上传命令、重复判断和三级限流。
8. 实现全局曲库推荐、近期去重和图文混合发送。
9. 实现软删除、上传者权限、全局管理员和来源群管理员查询。
10. 增加单元测试、网络模拟测试和数据库损坏/超时测试。
11. 在测试群试运行，只开放给一个群；确认无刷屏和误删后再扩大范围。

## 10. 验收标准

- 四组命令及别名都必须以 `/` 开头，无斜杠文本不会触发插件，参数错误不会进入普通聊天；
- 项目依赖中不存在 `bilibili-api-python`，固定 API 的原始 JSON 不泄漏到命令和数据库逻辑；
- 同一 B 站视频无论长链接、短链接或带查询参数，都只产生一条曲目记录；
- 重启 MaiBot 后曲库、删除状态和限流记录仍存在；
- 普通用户不能删除他人曲目，上传者可以删除自己的曲目；
- 群管理员权限查询失败时不会越权；
- 推荐消息顺序为“推荐、标题、封面、链接”，封面失败时仍有可点击链接；
- 有效曲目的缓存不会因时间自动删除，软删除曲目的缓存会按 30 天规则清理；
- 连续推荐不会频繁重复最近曲目；
- 并发上传同一首歌时数据库仍只有一条记录；
- 所有外部请求有超时、大小限制和域名校验；
- 插件停用或卸载不会影响 MaiBot 主数据库及其他插件。

## 11. 开工前采用的默认决定

若没有另行修改，实施时按以下默认值执行：

- 曲库确定为所有允许群共享的全局曲库；
- 任意用户可上传，每人 60 秒一次、每天最多 10 首；
- 上传后立即生效，不设人工审核；
- 删除为软删除；
- 所有命令必须以 `/` 开头，不提供裸命令；
- 上传者和配置中的全局管理员具有删除权限；来源群的群主/管理员也可删除本群投稿，但必须在来源群执行命令；
- 元数据直接请求固定的 B 站 `view` API，不增加第三方包；
- 有效曲目的封面长期缓存，软删除曲目封面保留 30 天，缓存上限 256 MiB；
- 推荐时避开当前聊天最近 5 首；
- 首次试运行通过 `allowed_group_ids` 只开放给测试群。

曲库范围和群管理员权限已经确定。实现前只需确认全局管理员 QQ 号和首个测试群号；它们属于部署配置，不影响代码结构。

## 12. 接口依据与版本风险

- [本次采用的 B 站视频信息接口样例](https://api.bilibili.com/x/web-interface/view?bvid=BV16veP6eEeC)：已实测 BVID/AID 查询、核心元数据字段、错误参数和封面下载。
- [B 站视频页面](https://www.bilibili.com/video/BV16veP6eEeC)：用于核对接口返回的标题和标准视频链接。

该接口位于 B 站官方域名，但不能把它视为永久稳定的公开契约。字段新增可以忽略；核心字段缺失、类型变化或业务错误必须安全失败。所有接口知识集中在 `bilibili_client.py` 和对应测试中，未来接口变更时只替换这一层。

## 13. 现有草稿与协作方式

`plugins/Chinese_vocaloid_recommender/` 已有 welken 编写的插件骨架、Manifest、推荐和上传命令起点。后续不整目录重写，而是在保留草稿的基础上分阶段修正和接线。

welken 参与约 25% 工作量的具体文件所有权、帮助等级、里程碑和第一批任务见同目录的 [`HUMAN_AI_COLLABORATION_PLAN.md`](HUMAN_AI_COLLABORATION_PLAN.md)。
