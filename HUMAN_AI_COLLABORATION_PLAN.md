# 中 V 推荐插件：人机协作与学习计划

## 1. 目的

目标不是由 AI 一次性生成整个插件，而是让 welken 实际负责约 25% 的功能工作，并能说清自己负责部分的输入、输出和失败情况。剩余高风险部分由 AI 实现，避免初学阶段被数据库并发、网络异常和权限边界拖住。

工作量不按代码行数计算，而按“独立负责并验收的功能”计算。格式化代码、生成重复结构不算主要贡献；亲自决定行为、写出可运行功能、发现问题并完成验收才算。

### 2026-09-22 进度

- M0：已完成。插件目录已初始化为独立 Git 仓库，MaiBot 官方父仓库继续忽略整个插件目录。
- M1：验收通过。`plugin.py` 语法、TOML、Manifest 和四个命令正则检查通过，帮助命令已完成。
- M2：welken 已完成字段审查；AI 按审查结果删除不展示的时长、投稿者、上传者昵称和点赞数字段，增加播放量 2 天按需刷新设计及并发重复上传测试。
- M3：已完成。welken 独立写出 B 站 JSON 到 `VideoMetadata` 的核心字段对应和两个合法链接样例；AI 补齐固定 API 请求、白名单、逐跳短链检查、总超时、响应上限和网络模拟测试。
- 文档：welken 已完成 README、CHANGELOG 和配置注释初稿；pair 阶段负责核对实现状态、版本和配置语义。
- 下一阶段：实现 M4 封面缓存；welken 负责核对成功/失败提示文案和 README 使用说明。
- 已修正：`allowed_group_ids` 与运行时 `group_id` 统一使用字符串。待 pair 修正：Manifest 的图文能力在命令接线时补齐。

## 2. 当前草稿的状态

现有目录和文件是有效的起点，不推倒重来：

| 文件 | 当前状态 | 后续处理 |
| --- | --- | --- |
| `_manifest.json` | 已有插件名称、版本、作者和基础能力 | 保留并修正仓库地址、插件 ID 风格和消息能力 |
| `plugin.py` | 已有配置骨架、推荐命令和上传命令入口 | 保留命令思路，清除残留签到逻辑后逐步接线 |
| `models.py` | 已完成并通过测试 | welken 审查字段含义，后续由 AI 维护 |
| `repository.py` | 已完成并通过测试 | 后续接入命令和权限层 |
| `cover_cache.py` | 空文件 | 由 AI 编写封面缓存层 |
| `permissions.py` | 空文件 | 由 AI 编写权限判断层 |
| `README.md` | 空文件 | 由 welken 主写 |
| `CHANGELOG.md` | 空文件 | 由 welken 维护 |
| `config.toml` | 已由 welken 创建并通过 TOML 解析 | 接线时补充网络和限流配置 |
| `bilibili_client.py` | JSON 映射、固定 API、超时、大小限制和链接白名单已完成 | 命令接线时复用，不把网络细节放进 `plugin.py` |
| `tests/` | 已有 20 项数据库、解析和模拟网络测试 | AI 继续补自动测试，welken 编写并执行手工测试清单 |

当前 `plugin.py` 已通过语法检查，命令骨架可以进入接线阶段。尚未完成的是实际上传、推荐和删除业务；这些占位函数返回成功但还不会操作曲库。Manifest 当前只有 `send.text`，图文能力会在 M4 接线时补齐。

## 3. 工作比例

### welken 主导：约 25%

| 工作包 | 占比 | 完成标志 |
| --- | ---: | --- |
| W1. 命令与配置 | 8% | 四组命令、配置默认值和 `config.toml` 可被插件加载 |
| W2. 帮助与回复体验 | 6% | 独立完成 `/中v帮助`，确定全部成功/失败文案 |
| W3. 文档与变更记录 | 5% | README 能让没看过代码的人安装和使用；CHANGELOG 记录版本 |
| W4. 真实群手工验收 | 6% | 按清单验证上传、重复、推荐、删除、越权和断网场景 |

### AI 主导：约 75%

| 工作包 | 占比 | 原因 |
| --- | ---: | --- |
| A1. SQLite 数据层与迁移 | 18% | 涉及事务、唯一约束、并发和数据恢复 |
| A2. B 站固定 API 客户端 | 12% | 涉及 URL 安全、超时、错误码和响应类型校验 |
| A3. 封面缓存 | 10% | 涉及原子文件写入、格式校验、容量和清理 |
| A4. 权限与限流 | 12% | 涉及上传者、跨群管理员和失败时的最小权限 |
| A5. 命令业务接线 | 11% | 把数据层、网络层和发送层组合起来 |
| A6. 自动测试与故障修复 | 12% | 覆盖并发、异常响应、权限和持久化 |

这个比例允许调整到 20%～30%，但不能通过把复杂模块整块丢给 welken 来“凑比例”。

## 4. 文件所有权

### welken 可直接修改

- `config.toml`
- `README.md`
- `CHANGELOG.md`
- `plugin.py` 中带有 `# HUMAN-OWNED` 标记的配置类、帮助命令和回复文案
- `manual_test_checklist.md`

### AI 可直接修改

- `models.py`
- `repository.py`
- `bilibili_client.py`
- `cover_cache.py`
- `permissions.py`
- `rate_limit.py`（若拆出）
- `tests/` 下的自动测试

### 共同修改

- `plugin.py` 的上传、推荐和删除处理函数
- `_manifest.json`

共同修改文件的规则：修改前先说明要改哪一段；保留 welken 已写的命令名、文案和明确设计决定；如果必须替换，先展示原因和差异。

## 5. AI 帮助等级

welken 每次可以指定一个帮助等级，避免 AI 直接给出完整答案：

1. `只审查`：只指出错误位置、现象和风险，不给改法。
2. `给提示`：说明下一步思路或指出可参考的现有插件，不写完整代码。
3. `给骨架`：提供函数签名、输入输出和伪代码，由 welken 填写主体。
4. `直接修复`：AI 完成代码、测试并解释关键点。

默认等级为 `给提示`。只有 AI 主导模块默认使用 `直接修复`；welken 主导模块未经明确同意不得整段重写。

## 6. 每轮协作流程

每轮只处理一个可在 20～60 分钟内完成的小任务：

1. AI 给出任务卡：目标、允许修改的文件、输入、输出和验收方式。
2. welken 先写第一版，可以不完整，但要标出最不确定的地方。
3. AI 按所选帮助等级审查，只处理本轮范围。
4. welken 至少亲自完成一次修改后，再决定是否让 AI 收尾。
5. 运行检查并记录结果。
6. 完成一个小提交，提交信息注明 `human`、`ai` 或 `pair`。

建议提交格式：

```text
human: add plugin config defaults
human: write help command and replies
ai: implement sqlite repository
pair: connect upload command to repository
```

## 7. 保护已有成果

当前仓库的 `.gitignore` 默认忽略 `plugins/*`，因此这个插件目录还没有 Git 版本保护。正式改代码前应先完成：

1. 在 `.gitignore` 中单独放行 `plugins/Chinese_vocaloid_recommender/`；
2. 把当前草稿作为第一份基线提交，明确它由 welken 创建；
3. 后续每个工作包单独提交，避免一次修改覆盖全部文件；
4. AI 不使用重置、回退或整目录替换来处理草稿。

这样即使后续实现方向改变，welken 的初始骨架和每次修改仍能从历史中看到。

## 8. 建议里程碑

### M0：保存草稿

负责人：共同。

- 放行插件目录的 Git 跟踪；
- 保存当前草稿；
- 不先修代码，确保初始成果可追溯。

### M1：可加载的空插件

负责人：welken 主写，AI 给提示和审查。

- 补完五个配置类的字段与默认值；
- 创建 `config.toml`；
- 修正 Manifest 的基本信息和能力；
- 删除或隔离签到残留逻辑；
- 让插件通过语法检查并能加载；
- 完成 `/中v帮助`。

这是 welken 最主要的一段代码贡献。它不要求理解数据库或异步并发，但会接触 Python 类型、类、配置和命令处理。

### M2：曲库核心

负责人：AI 主写，welken 审查数据字段名称。

- 完成 `models.py` 和 `repository.py`；
- 初始化 SQLite；
- 实现新增、重复查询、随机查询和软删除；
- 写自动测试。

welken 需要审查：数据库保存的信息是否符合实际管理需求，ID 和删除行为是否直观。

### M3：B 站信息与封面

负责人：AI 主写，welken 提供真实样例。

- 完成链接解析和固定 `view` API 客户端；
- 完成封面下载、缓存和清理；
- 对正常、失效、短链、多分 P 等样例测试。

welken 负责提供至少 5 个不同形式的实际投稿链接，并判断插件展示的标题和封面是否符合预期。

### M4：上传、推荐和删除

负责人：共同。

- AI 接通数据库、网络、权限和限流；
- welken 决定每种结果的最终回复；
- welken 亲自完成至少一个命令处理函数中的文案分支；
- 双方检查所有命令都以 `/` 开头。

### M5：测试群验收

负责人：welken 主导。

- 只在测试群开放；
- 按手工清单运行；
- 记录命令、实际结果和是否通过；
- AI 修复代码问题，welken复测并决定是否发布。

## 9. welken 的第一批任务

第一轮建议只做以下三件事，不碰数据库和网络：

### 任务 W1-1：填写配置含义

在纸面或 `config.toml` 中亲自确定这些值：

- 最近避免重复推荐几首；
- 推荐冷却秒数；
- 上传冷却秒数和每日上限；
- 封面缓存上限；
- 哪些群先开放；
- 全局管理员 QQ 号；
- 是否允许私聊上传。

### 任务 W1-2：完成帮助命令

输入：`/中v帮助`。

输出：只发送一次简短帮助文本。

验收：函数调用 `self.ctx.send.text(...)`，并返回 `(True, 摘要, True)`；无 `/` 的“中v帮助”不会触发。

遇到困难时，先参考 `plugins/hide_checkin/plugin.py` 的命令结构，只看命令装饰器、发送和返回三元组，不复制签到业务。

### 任务 W2-1：确定回复文案

亲自填写下表，后续 AI 只接线，不改语气：

| 情况 | 最终文案 |
| --- | --- |
| 上传成功 | 待填写 |
| 已经收录 | 待填写 |
| 链接无效 | 待填写 |
| 上传过快 | 待填写 |
| 曲库为空 | 待填写 |
| 删除成功 | 待填写 |
| 找不到 ID | 待填写 |
| 没有权限 | 待填写 |
| B 站接口暂时失败 | 待填写 |

## 10. AI 审查当前代码时的边界

AI 可以立即指出语法错误、未定义名称、安全问题和测试缺口，但在 welken 主导区域遵守以下规则：

- 不把 `plugin.py` 整文件替换为重新生成版本；
- 不擅自改变命令名、回复口吻和已确定的权限规则；
- 删除签到残留前先列出将删除的代码段；
- 每次修改后说明“保留了什么、改变了什么、为什么”；
- 自动测试失败时先解释失败行为，再修复；
- 不用“最佳实践”作为无理由扩大实现范围的借口。

## 11. 学习目标

完成插件后，welken 不需要掌握所有底层细节，但应能够独立回答：

- 一个 `@Command` 如何匹配命令并拦截普通聊天；
- `stream_id`、`user_id`、`group_id` 分别用于什么；
- 配置模型和 `config.toml` 如何对应；
- 上传、推荐和删除分别调用了哪些模块；
- 为什么随机推荐平时不访问 B 站接口；
- 为什么群管理员只能删除来源群投稿；
- SQLite 文件和封面目录在哪里，如何备份；
- 遇到异常时先看哪段日志、运行哪个测试。

如果这些问题能够回答，25% 的参与就不是形式上的代码占比，而是对插件关键行为拥有真实理解和控制权。

## 12. 完成标准

- welken 至少独立完成配置、帮助命令、回复文案和 README；
- welken 至少亲自修正一次 AI 审查发现的问题；
- welken 主导完整的测试群验收并决定是否发布；
- Git 历史能区分 human、ai 和 pair 的提交；
- AI 主导模块都有自动测试和简明接口说明；
- 最终代码中不存在无法解释用途的整段生成代码；
- 后续修改命令、限流、管理员或回复时，welken 可以不依赖 AI 独立完成。
